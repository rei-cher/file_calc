# Instruction
## 1. Build
Run the build script
```
./build.sh
```
## Execute
To execute the program navigate to the 'build' directory and run 'simplecalc' with desired parameters
```
cd build
./filecalc <dir_with_unsolved_equ_files> <dir_where_solved_equ_files_will_be_stored>
```
